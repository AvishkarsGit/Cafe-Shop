<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ProductSave</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-100% bg-zinc-900 p-10 text-white">
        <div class="nav mb-5">
            <a class="text-blue-500" href="/">Adding Product</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">All Products</h1>

        <div class="users flex gap-3 flex-wrap">
           <% if(cafeproduct.length > 0) {  %>
            <% cafeproduct.forEach(function(product){%>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-72 bg-zinc-700 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-cover object-top" src="<%=product.imgUrl  %>" alt="">
                    </div>
                    <h3 class="text-2xl mt-2"><%= product.ProductName  %></h3>
                    <h3 class="text-xl mt-3"><%= product.ProductPrice %></h3>
                    <h5 class="text-zinc-500"><%= product.Description%></h5>
                    <div class="flex gap-3 mt-10">
                        <a class="text-zinc-200" href="/edit/<%= product._id %>">Edit Product</a>
                        <a class="text-red-500" href="/delete/<%= product._id %>">Delete Product</a>
                    </div>
                </div>
               <% }) %>
           <% } else{  %>
             <h3 class="text-zinc-500" >No one Product here</h3>
           <% } %>
           

          

        </div>

    </div>
</body>

</html>